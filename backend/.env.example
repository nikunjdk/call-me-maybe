# Copy to .env. Do not commit .env.

HOST=0.0.0.0
PORT=8000

# A's dial hook — C will POST here on approve (not wired in hour one)
DIAL_WEBHOOK_URL=

# Member A / Twilio — fill locally, never commit .env
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_API_KEY=
TWILIO_API_SECRET=
TWILIO_TWIML_APP_SID=
TWILIO_CALLER_NUMBER=
TWILIO_SIM_REP_NUMBER=
TWILIO_LIVE_REP_NUMBER=
REP_MODE=scripted
PUBLIC_BASE_URL=
TWILIO_VALIDATE_SIGNATURE=false

# Provider roles (TIER1_ / TIER2_ / PLANNER_ / EXTRACTOR_) land with the
# llm/provider module next. IFM live + self-hosted commented underneath.
