NEXT_PUBLIC_API_BASE=http://localhost:8000

# auto = replay the demo tape if C sends no call events after approve
# 1 = always replay; 0 = never replay
NEXT_PUBLIC_FAKE_EVENTS=auto

# Set to false and fill Auth0 vars to require sign-in
AUTH_DISABLED=true

AUTH0_SECRET=
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
APP_BASE_URL=http://localhost:3000
